import { Center, Spinner as Sp } from "@chakra-ui/react";
import React from "react";

const Spinner = () => {
  return (
    <Center>
      <Sp size="xl" />
    </Center>
  );
};

export default Spinner;
